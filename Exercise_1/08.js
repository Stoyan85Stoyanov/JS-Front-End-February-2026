function reversedChars(a, b, c) {
    
    const arr = [a, b, c];
    arr.reverse();
    console.log(arr.join(" "));
     
}

//    const result = `${c} ${b} ${a}`;
//     console.log(result);


reversedChars('A','B','C');
reversedChars('1','L','&');